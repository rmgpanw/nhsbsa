% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/package.R
\name{nhsbsa_package_show}
\alias{nhsbsa_package_show}
\title{Show a dataset's metadata}
\usage{
nhsbsa_package_show(id, .return_raw = FALSE)
}
\arguments{
\item{id}{Character scalar. The dataset identifier or name, as returned by
\code{\link[=nhsbsa_package_list]{nhsbsa_package_list()}}.}

\item{.return_raw}{Logical. If \code{TRUE}, return the full parsed CKAN response
envelope instead of the processed result. Defaults to \code{FALSE}.}
}
\value{
A list of dataset metadata. With \code{.return_raw = TRUE}, the parsed
response envelope as a list.
}
\description{
Wraps the CKAN \code{package_show} action to return the full metadata for a single
dataset, including its list of resources (downloadable files and datastore
tables).
}
\examples{
\dontshow{if (identical(Sys.getenv("IN_PKGDOWN"), "true")) withAutoprint(\{ # examplesIf}
metadata <- nhsbsa_package_show("english-prescribing-data-epd")
metadata$title
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=nhsbsa_list_resources]{nhsbsa_list_resources()}} for a tidy table of a dataset's resources.
}
