% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/package.R
\name{nhsbsa_package_list}
\alias{nhsbsa_package_list}
\title{List available datasets}
\usage{
nhsbsa_package_list(.return_raw = FALSE)
}
\arguments{
\item{.return_raw}{Logical. If \code{TRUE}, return the full parsed CKAN response
envelope instead of the processed result. Defaults to \code{FALSE}.}
}
\value{
A character vector of dataset identifiers. With \code{.return_raw = TRUE},
the parsed response envelope as a list.
}
\description{
Wraps the CKAN \code{package_list} action to return the identifiers of every
dataset published on the NHSBSA Open Data Portal.
}
\examples{
\dontshow{if (identical(Sys.getenv("IN_PKGDOWN"), "true")) withAutoprint(\{ # examplesIf}
datasets <- nhsbsa_package_list()
length(datasets)
head(datasets)
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=nhsbsa_package_show]{nhsbsa_package_show()}} for a dataset's metadata,
\code{\link[=nhsbsa_package_search]{nhsbsa_package_search()}} to search datasets.
}
