% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/catalogue.R
\name{nhsbsa_tag_list}
\alias{nhsbsa_tag_list}
\title{List tags}
\usage{
nhsbsa_tag_list(query = NULL, vocabulary_id = NULL, .return_raw = FALSE)
}
\arguments{
\item{query}{Character scalar. Restrict the results to tags containing this
string.}

\item{vocabulary_id}{Character scalar. Restrict the results to tags in a
particular CKAN tag vocabulary.}

\item{.return_raw}{Logical. If \code{TRUE}, return the full parsed CKAN response
envelope instead of the processed result. Defaults to \code{FALSE}.}
}
\value{
A character vector of tags. With \code{.return_raw = TRUE}, the parsed
response envelope as a list.
}
\description{
Wraps the CKAN \code{tag_list} action to list the tags applied to datasets. These
are the same tags shown on the portal website; you can filter datasets by a
tag with \code{\link[=nhsbsa_package_search]{nhsbsa_package_search()}} (see \code{vignette("nhsbsa")}).
}
\examples{
\dontshow{if (identical(Sys.getenv("IN_PKGDOWN"), "true")) withAutoprint(\{ # examplesIf}
# All tags
tags <- nhsbsa_tag_list()
length(tags)

# Only tags containing a given string
nhsbsa_tag_list(query = "prescribing")
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=nhsbsa_package_search]{nhsbsa_package_search()}} to find datasets by tag.
}
