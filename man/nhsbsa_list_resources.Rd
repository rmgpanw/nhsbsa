% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/resources.R
\name{nhsbsa_list_resources}
\alias{nhsbsa_list_resources}
\title{List a dataset's resources as a tibble}
\usage{
nhsbsa_list_resources(dataset_id, pattern = NULL)
}
\arguments{
\item{dataset_id}{Character scalar. The dataset identifier, as returned by
\code{\link[=nhsbsa_package_list]{nhsbsa_package_list()}}.}

\item{pattern}{Character scalar. An optional regular expression; only
resources whose \code{name} matches (case-insensitively) are returned.}
}
\value{
A tibble with one row per resource and columns \code{name}, \code{id},
\code{format}, \code{created}, \code{last_modified}, \code{url} and \code{size}.
}
\description{
A convenience wrapper around \code{\link[=nhsbsa_package_show]{nhsbsa_package_show()}} that returns a dataset's
resources as a tibble, optionally filtered by a pattern matched against the
resource name. This is the most direct way to discover the resources (and
their download URLs) available for a dataset.
}
\details{
If you have already fetched the dataset metadata with \code{\link[=nhsbsa_package_show]{nhsbsa_package_show()}},
calling \code{\link[tibble:as_tibble]{tibble::as_tibble()}} on it gives the same table without a second
request (but without the \code{pattern} filter).
}
\examples{
\dontshow{if (identical(Sys.getenv("IN_PKGDOWN"), "true")) withAutoprint(\{ # examplesIf}
# All resources for a dataset
nhsbsa_list_resources("english-prescribing-data-epd")

# Only resources whose name matches a pattern
nhsbsa_list_resources("english-prescribing-data-epd", pattern = "202401")
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=nhsbsa_download_resource]{nhsbsa_download_resource()}} to download a resource file;
\code{\link[=nhsbsa_package_show]{nhsbsa_package_show()}} (with \code{\link[tibble:as_tibble]{tibble::as_tibble()}}) for the same table from
already-fetched metadata.
}
