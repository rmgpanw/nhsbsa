% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/datastore.R
\name{nhsbsa_datastore_search_sql}
\alias{nhsbsa_datastore_search_sql}
\title{Query a resource with SQL}
\usage{
nhsbsa_datastore_search_sql(resource_id, sql, .return_raw = FALSE)
}
\arguments{
\item{resource_id}{Character scalar. The resource the query targets. The
NHSBSA datastore requires this alongside \code{sql}, and identifies a resource by
its \emph{name} (the \code{name} column of \code{\link[=nhsbsa_list_resources]{nhsbsa_list_resources()}}), e.g.
\code{"EPD_201401"}. Reference the same name in the query's \code{FROM} clause.}

\item{sql}{Character scalar. A single read-only SQL \code{SELECT} statement, e.g.
\code{'SELECT * FROM `EPD_201401` LIMIT 10'}.}

\item{.return_raw}{Logical. If \code{TRUE}, return the full parsed CKAN response
envelope instead of the processed result. Defaults to \code{FALSE}.}
}
\value{
A tibble with one row per record returned by the query. With
\code{.return_raw = TRUE}, the parsed response envelope as a list.
}
\description{
Wraps the CKAN \code{datastore_search_sql} action, which runs a read-only SQL query
against the datastore. The \code{sql} string is sent to the API verbatim; paging is
the caller's responsibility (via \code{LIMIT}/\code{OFFSET} in the query).
}
\examples{
\dontshow{if (identical(Sys.getenv("IN_PKGDOWN"), "true")) withAutoprint(\{ # examplesIf}
# Select specific columns
nhsbsa_datastore_search_sql(
  resource_id = "EPD_202401",
  sql = "SELECT YEAR_MONTH, PCO_CODE, ITEMS FROM `EPD_202401` LIMIT 10"
)

# Aggregate server-side: total items prescribed per organisation
nhsbsa_datastore_search_sql(
  resource_id = "EPD_202401",
  sql = "SELECT PCO_CODE, SUM(ITEMS) AS items
         FROM `EPD_202401`
         GROUP BY PCO_CODE
         ORDER BY items DESC
         LIMIT 10"
)
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=nhsbsa_datastore_search]{nhsbsa_datastore_search()}} for a parameterised search.
}
