% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/package.R
\name{nhsbsa_package_search}
\alias{nhsbsa_package_search}
\title{Search datasets}
\usage{
nhsbsa_package_search(
  q = NULL,
  fq = NULL,
  sort = NULL,
  rows = NULL,
  start = NULL,
  .return_raw = FALSE
)
}
\arguments{
\item{q}{Character scalar. The Solr query string (e.g. \code{"prescribing"}).
Defaults to \code{NULL} (match all).}

\item{fq}{Character scalar. A Solr filter query.}

\item{sort}{Character scalar. Sort order, e.g. \code{"metadata_modified desc"}.}

\item{rows}{Integer. Maximum number of datasets to return.}

\item{start}{Integer. Offset into the result set, for paging.}

\item{.return_raw}{Logical. If \code{TRUE}, return the full parsed CKAN response
envelope instead of the processed result. Defaults to \code{FALSE}.}
}
\value{
A list with the search \code{count} and matching datasets in \code{results}.
With \code{.return_raw = TRUE}, the parsed response envelope as a list.
}
\description{
Wraps the CKAN \code{package_search} action, a Solr-backed search over datasets.
}
\examples{
\dontshow{if (identical(Sys.getenv("IN_PKGDOWN"), "true")) withAutoprint(\{ # examplesIf}
hits <- nhsbsa_package_search(q = "prescribing", rows = 5)
hits$count
\dontshow{\}) # examplesIf}
}
