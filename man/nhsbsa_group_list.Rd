% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/catalogue.R
\name{nhsbsa_group_list}
\alias{nhsbsa_group_list}
\title{List dataset groups}
\usage{
nhsbsa_group_list(all_fields = NULL, .return_raw = FALSE)
}
\arguments{
\item{all_fields}{Logical. If \code{TRUE}, return a richer record for each
organisation rather than just its name. Defaults to the API default
(\code{FALSE}) when left \code{NULL}.}

\item{.return_raw}{Logical. If \code{TRUE}, return the full parsed CKAN response
envelope instead of the processed result. Defaults to \code{FALSE}.}
}
\value{
A character vector of group names. If \code{all_fields = TRUE}, a list of
group records. With \code{.return_raw = TRUE}, the parsed response envelope as a
list.
}
\description{
Wraps the CKAN \code{group_list} action to list the groups (thematic collections)
that datasets can belong to.
}
\examples{
\dontshow{if (identical(Sys.getenv("IN_PKGDOWN"), "true")) withAutoprint(\{ # examplesIf}
nhsbsa_group_list()
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=nhsbsa_organization_list]{nhsbsa_organization_list()}}, \code{\link[=nhsbsa_tag_list]{nhsbsa_tag_list()}}.
}
